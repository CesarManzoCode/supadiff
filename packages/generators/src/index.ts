/**
 * @supadiff/generators — domain model and seeded scenario producers
 * (Architecture Contract §10, Implementation DAG L12).
 *
 * Out of scope for this delivery (L0-L5). Generation is explicitly deferred until
 * deterministic comparison, repro, and upgrade behavior are proven (C-001). Not
 * implemented here; `fast-check` is intentionally not a dependency yet (§10.1).
 */
export const GENERATORS_PACKAGE_STATUS = "not-implemented-pending-L12" as const;
