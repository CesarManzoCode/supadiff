{
  "name": "@supadiff/generators",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0 <23.0.0"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:live-smoke": "vitest run --config vitest.live-smoke.config.ts"
  },
  "dependencies": {
    "@supadiff/spec": "workspace:*",
    "fast-check": "4.9.0"
  },
  "devDependencies": {
    "@supadiff/engine": "workspace:*",
    "@supadiff/targets": "workspace:*"
  }
}
