/**
 * @supadiff/targets — concrete target and transition drivers (Architecture Contract §13.2).
 *
 * Out of scope for this delivery (L0-L5). Concrete Supalite/Supabase drivers are
 * Implementation DAG layers L6-L8 and are intentionally not implemented here.
 * This package exists as a boundary-compliant placeholder so the workspace graph,
 * boundary checker, and toolchain are exercised end to end.
 */
export const TARGETS_PACKAGE_STATUS = "not-implemented-pending-L6" as const;
