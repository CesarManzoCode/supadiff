# SupaDiff v1.0.0 — release evidence

Stable content hash: `sha256:081796ce2c72a498440db9639cca8dd805a9050eeb677c7b38ea3c49132c694c`
Release-inputs digest: `sha256:9e365bec5e4cc0a18c85f53a8cc1e28000f401e835bce452788dd73ec053d67f`

This manifest is produced from the repository working tree by
`pnpm release:evidence` and re-verified on every run. No secrets, no
fake-target result presented as real Supabase/Supalite evidence.

**Release revision.** This file records no commit SHA: committing it changes `HEAD`, so no
hash written here could be the final release commit. The authoritative v1.0.0 revision is the
commit the `v1.0.0` git tag and GitHub Release point to. Generation-time provenance (branch `feat/v1-completion`, working tree clean at 2026-09-04T02:08:48.160Z) is non-canonical and excluded from the stable hash.

## Toolchain / pinned versions

- `node`: >=22.0.0 <23.0.0
- `packageManager`: pnpm@9.15.0
- `typescript`: 5.7.2
- `vitest`: 2.1.8
- `supabaseCli`: 2.116.0
- `@supabase/lite`: 0.9.0
- `@supabase/supabase-js`: 2.97.0
- `postgres`: 3.4.8
- `fast-check`: 4.9.0

## Pinned supabase-local images

- `postgres`: `public.ecr.aws/supabase/postgres:17.6.1.165`
- `gotrue`: `public.ecr.aws/supabase/gotrue:v2.196.0`
- `postgrest`: `public.ecr.aws/supabase/postgrest:v16.1`
- `storage-api`: `public.ecr.aws/supabase/storage-api:v1.70.3`
- `kong`: `public.ecr.aws/supabase/kong:2.8.1`

## Target capability matrix

### `supabase-hosted` — real
_public API + Supabase Management API_

- `auth.password.signin` → **exact**
- `auth.password.signup` → **exact**
- `auth.session.read` → **exact**
- `auth.session.refresh` → **exact**
- `auth.session.revoke` → **exact**
- `auth.user.update` → **exact**
- `cli.invoke` → **exact**
- `cli.projectTree.read` → **exact**
- `data.delete` → **exact**
- `data.insert` → **exact**
- `data.seed` → **exact**
- `data.select` → **exact**
- `data.update` → **exact**
- `data.upsert` → **exact**
- `execution.controlled-concurrency` → **unsupported**
- `http.preflight` → **exact**
- `migration.apply` → **exact**
- `rls.emulated.with-check` → **unsupported**
- `rls.native` → **exact**
- `schema.apply` → **exact**
- `schema.introspect` → **exact**
- `storage.bucket.create` → **exact**
- `storage.object.read` → **exact**
- `storage.object.write` → **exact**
- `storage.signed-url.create` → **exact**
- `storage.signed-url.redeem` → **exact**

### `supabase-local` — real
_pinned supabase CLI 2.116.0 over Docker Compose_

- `auth.password.signin` → **exact**
- `auth.password.signup` → **exact**
- `auth.session.read` → **exact**
- `auth.session.refresh` → **exact**
- `auth.session.revoke` → **exact**
- `auth.user.update` → **exact**
- `cli.invoke` → **exact**
- `cli.projectTree.read` → **exact**
- `data.delete` → **exact**
- `data.insert` → **exact**
- `data.seed` → **exact**
- `data.select` → **exact**
- `data.update` → **exact**
- `data.upsert` → **exact**
- `execution.controlled-concurrency` → **unsupported**
- `http.preflight` → **exact**
- `migration.apply` → **exact**
- `rls.emulated.with-check` → **unsupported**
- `rls.native` → **exact**
- `schema.apply` → **exact**
- `schema.introspect` → **exact**
- `storage.bucket.create` → **exact**
- `storage.object.read` → **exact**
- `storage.object.write` → **exact**
- `storage.signed-url.create` → **exact**
- `storage.signed-url.redeem` → **exact**

### `supalite-sqlite` — real
_real @supabase/lite@0.9.0 lite start subprocess_

- `auth.password.signin` → **unsupported**
- `auth.password.signup` → **unsupported**
- `auth.session.read` → **unsupported**
- `auth.session.refresh` → **unsupported**
- `auth.session.revoke` → **unsupported**
- `auth.user.update` → **unsupported**
- `cli.invoke` → **exact**
- `cli.projectTree.read` → **exact**
- `data.delete` → **exact**
- `data.insert` → **exact**
- `data.seed` → **exact**
- `data.select` → **exact**
- `data.update` → **exact**
- `data.upsert` → **exact**
- `execution.controlled-concurrency` → **unsupported**
- `http.preflight` → **exact**
- `migration.apply` → **exact**
- `rls.emulated.with-check` → **unsupported**
- `rls.native` → **unsupported**
- `schema.apply` → **exact**
- `schema.apply.declarative-pg-dialect` → **unsupported**
- `schema.introspect` → **exact**
- `storage.bucket.create` → **unsupported**
- `storage.object.read` → **unsupported**
- `storage.object.write` → **unsupported**
- `storage.signed-url.create` → **unsupported**
- `storage.signed-url.redeem` → **unsupported**

### `supalite-sqlite-postgres` — real
_real @supabase/lite@0.9.0 lite start subprocess_

- `auth.password.signin` → **exact**
- `auth.password.signup` → **exact**
- `auth.session.read` → **exact**
- `auth.session.refresh` → **exact**
- `auth.session.revoke` → **exact**
- `auth.user.update` → **exact**
- `cli.invoke` → **exact**
- `cli.projectTree.read` → **exact**
- `data.delete` → **exact**
- `data.insert` → **exact**
- `data.seed` → **exact**
- `data.select` → **exact**
- `data.update` → **exact**
- `data.upsert` → **exact**
- `execution.controlled-concurrency` → **unsupported**
- `http.preflight` → **exact**
- `migration.apply` → **exact**
- `rls.emulated.with-check` → **approximation**
- `rls.native` → **unsupported**
- `schema.apply` → **exact**
- `schema.introspect` → **exact**
- `storage.bucket.create` → **experimental**
- `storage.object.read` → **experimental**
- `storage.object.write` → **experimental**
- `storage.signed-url.create` → **experimental**
- `storage.signed-url.redeem` → **unsupported**

### `supalite-pglite` — real
_real @supabase/lite@0.9.0 lite start subprocess_

- `auth.password.signin` → **exact**
- `auth.password.signup` → **exact**
- `auth.session.read` → **exact**
- `auth.session.refresh` → **exact**
- `auth.session.revoke` → **exact**
- `auth.user.update` → **exact**
- `cli.invoke` → **exact**
- `cli.projectTree.read` → **exact**
- `data.delete` → **exact**
- `data.insert` → **exact**
- `data.seed` → **exact**
- `data.select` → **exact**
- `data.update` → **exact**
- `data.upsert` → **exact**
- `execution.controlled-concurrency` → **unsupported**
- `http.preflight` → **exact**
- `migration.apply` → **exact**
- `rls.emulated.with-check` → **unsupported**
- `rls.native` → **exact**
- `schema.apply` → **exact**
- `schema.introspect` → **exact**
- `storage.bucket.create` → **experimental**
- `storage.object.read` → **experimental**
- `storage.object.write` → **experimental**
- `storage.signed-url.create` → **experimental**
- `storage.signed-url.redeem` → **unsupported**

### `supalite-postgres` — real
_real @supabase/lite@0.9.0 lite start subprocess + local PostgreSQL_

- `auth.password.signin` → **exact**
- `auth.password.signup` → **exact**
- `auth.session.read` → **exact**
- `auth.session.refresh` → **exact**
- `auth.session.revoke` → **exact**
- `auth.user.update` → **exact**
- `cli.invoke` → **exact**
- `cli.projectTree.read` → **exact**
- `data.delete` → **exact**
- `data.insert` → **exact**
- `data.seed` → **exact**
- `data.select` → **exact**
- `data.update` → **exact**
- `data.upsert` → **exact**
- `execution.controlled-concurrency` → **unsupported**
- `http.preflight` → **exact**
- `migration.apply` → **exact**
- `rls.emulated.with-check` → **unsupported**
- `rls.native` → **exact**
- `schema.apply` → **exact**
- `schema.introspect` → **exact**
- `storage.bucket.create` → **experimental**
- `storage.object.read` → **experimental**
- `storage.object.write` → **experimental**
- `storage.signed-url.create` → **experimental**
- `storage.signed-url.redeem` → **unsupported**

### `fake` — test-infrastructure-only
_scripted FakeTargetDriver — NEVER evidence about Supabase or Supalite (§15.2)_

- (no capabilities — test infrastructure only)

## Active divergence registry

- `div.lite-upgrade-local-sequence-not-reset` (active) — `lite upgrade --target local` from a file-backed Supalite source does not carry the serial-sequence position, so the first post-upgrade insert collides
- `div.supalite-signed-url-key-name-length` (active) — Supalite 0.9.0 signedUrl/signedURL bug: redeemed content length also differs across targets
- `div.supalite-signed-url-key-name` (active) — Supalite 0.9.0 sign-URL response uses `signedUrl`, breaking redemption through the official client

## Acceptance gates — recorded results

Each result below was produced by `pnpm release:acceptance` executing the exact command
shown and recording its real exit code + a sanitized copy of the full output as a
content-addressed artifact. `pnpm release:evidence` re-verifies every digest.

### `pnpm check` (core)

- result: **pass** (exit 0, executed: true)
- target identity: none (hermetic — no real target, no network)
- evidence artifact: `release-evidence/acceptance/core.log`
- artifact digest: `sha256:bd549205de15a83ed7518602755aad59b294f7f83cf5c08b950f68279af35d1e`
- limitations: Hermetic; no real target, no network. Includes the management-plane fault suite.

### `pnpm test:integration:supalite` (L6)

- result: **pass** (exit 0, executed: true)
- target identity: real @supabase/lite@0.9.0 subprocess
- evidence artifact: `release-evidence/acceptance/L6.log`
- artifact digest: `sha256:40bde0b97a4f28d2fb8f5cc27ac567e88cb6592b46c830ac030558dcf07ac93e`
- limitations: `supalite-postgres` runs only when a local admin PostgreSQL is reachable at SUPADIFF_TEST_POSTGRES_ADMIN_URL; otherwise that one backend self-skips (recorded in the log).

### `pnpm test:integration:peer-data-auth-rls` (L7)

- result: **pass** (exit 0, executed: true)
- target identity: real @supabase/lite@0.9.0 ↔ supabase CLI 2.116.0 (pinned images) over Docker
- evidence artifact: `release-evidence/acceptance/L7.log`
- artifact digest: `sha256:638bccf3e37e332db273e2eb126663324db348fa70c3fc269fad45d962f29f57`
- limitations: Requires Docker; brings up the pinned supabase-local stack.

### `pnpm test:integration:upgrade-local` (L8)

- result: **pass** (exit 0, executed: true)
- target identity: real @supabase/lite@0.9.0 ↔ supabase CLI 2.116.0 (pinned images) over Docker
- evidence artifact: `release-evidence/acceptance/L8.log`
- artifact digest: `sha256:171c274ef3733f4f5688d57a5e66bfa53924ab4cebdaea4b1d15831aef569787`
- limitations: Requires Docker. `--target hosted` transitions are out of scope.

### `pnpm test:fault-lab:replay` (L9)

- result: **pass** (exit 0, executed: true)
- target identity: none (hermetic — no real target, no network)
- evidence artifact: `release-evidence/acceptance/L9.log`
- artifact digest: `sha256:8824656d4c26e70b13549635eb4a39c5ba2c8b4d7a3cc027f9e539d28dc94582`
- limitations: Hermetic.

### `pnpm test:fault-lab:reduce` (L10)

- result: **pass** (exit 0, executed: true)
- target identity: none (hermetic — no real target, no network)
- evidence artifact: `release-evidence/acceptance/L10.log`
- artifact digest: `sha256:14b199297d4e6b5767c891bbc0dc9b2d492060f875404a24222af38a744c7f60`
- limitations: Hermetic. Scoped to SupaDiff's own Data+Auth+RLS domain model.

### `pnpm test:integration:peer-storage` (L11)

- result: **pass** (exit 0, executed: true)
- target identity: real @supabase/lite@0.9.0 ↔ supabase CLI 2.116.0 (pinned images) over Docker
- evidence artifact: `release-evidence/acceptance/L11.log`
- artifact digest: `sha256:89a7856fe298872cfde054e3fe6e1b68c3b93c042af62d3f2072055ded82a4c7`
- limitations: Requires Docker for the Supalite ↔ supabase-local pair.

### `pnpm test:generators` (L12)

- result: **pass** (exit 0, executed: true)
- target identity: none (hermetic — no real target, no network)
- evidence artifact: `release-evidence/acceptance/L12.log`
- artifact digest: `sha256:93c96532baef7e3f2113fc9192c717d0e6499c828353c7eb5101bb434fb3bccd`
- limitations: Hermetic. Scoped to SupaDiff's own domain model, not a general-purpose generator.

### `pnpm test:generated-smoke` (L12-smoke)

- result: **pass** (exit 0, executed: true)
- target identity: real @supabase/lite@0.9.0 subprocess
- evidence artifact: `release-evidence/acceptance/L12-smoke.log`
- artifact digest: `sha256:02e32f69fe890011eb921f3bebf57c4af95b0eb63a82ec031bcb4fdd3bbe455c`
- limitations: Runs a single generated scenario against a real Supalite target.

### `SUPADIFF_HOSTED=1 pnpm test:integration:hosted-smoke` (L13)

- result: **pass** (exit 0, executed: true)
- target identity: real supabase-hosted throwaway smoke project · postgres 17 · ref-fingerprint 90815847e84b600a
- evidence artifact: `release-evidence/acceptance/L13.log`
- artifact digest: `sha256:cb1d311ad0a8726cf40457cea4634b7c485bd4440fa23424a5cfffb92b77ddc4`
- notes: 1 test(s) reported skipped by vitest — the fail-closed credential-precondition block (inert when hosted credentials are present); all 13 real hosted tests ran
- limitations: Requires the dedicated throwaway smoke project's credentials (SUPADIFF_HOSTED_ACCESS_TOKEN, SUPADIFF_HOSTED_PROJECT_REF). Fail-closed: with SUPADIFF_HOSTED=1 and no credentials the command exits non-zero rather than skipping. Cleanup proves the measured owned-resource census returns to the pre-run empty state, not a byte-for-byte project image. `create-ephemeral` and `--target hosted` upgrades are not exercised. Runs entirely over the live hosted API/Management API, so a transient upstream fault during runtime capability probing can finalize a single attempt `unsupported` (infrastructure outcome, not a behavioral result); the recorded result is from an attempt that completed cleanly.

### `pnpm docs:verify` (L14-docs)

- result: **pass** (exit 0, executed: true)
- target identity: none (hermetic — no real target, no network)
- evidence artifact: `release-evidence/acceptance/L14-docs.log`
- artifact digest: `sha256:683e616be187ea5615b532719e0a6241c817875d21cbdbc23bfb7f6c452e1c2f`
- limitations: Hermetic; reads committed files + built dist only.

### `pnpm release:evidence` (L14-evidence)

- result: **self-verifying** (exit null, executed: false)
- target identity: none (hermetic — verifies the other recorded results)
- evidence artifact: `null`
- artifact digest: `null`
- notes: This gate is `pnpm release:evidence` itself; it re-verifies every other recorded result on each run. Its own pass/fail is the exit status of that invocation.
- limitations: Verifies the recorded acceptance results for consistency, non-failure, and freshness against the release inputs digest; it does not itself re-execute the gates.

## Explicit unproven / unsupported surfaces

- Realtime, Edge Functions, and any dashboard/UI — never in scope (Architecture Contract §20).
- supabase-hosted `create-ephemeral` attach mode: implemented and safety-gated, but not exercised against a real org in CI (requires SUPADIFF_HOSTED_ORG_ID + billing). Only `attach-explicit` has a passing real acceptance gate.
- Hosted `lite upgrade --target hosted` transitions are not exercised (L8 covers `--target local` only).
- supabase-hosted `auth.signUp` uses the real GoTrue admin API + real password grant (not the public mailer flow) because the dedicated smoke project has no SMTP and the project-scoped token cannot toggle mailer autoconfirm — see docs/adr/0003-hosted-signup-via-admin-api.md.
- The L10 reducer and L12 generator are scoped to SupaDiff's own Data+Auth+RLS domain model, not general-purpose tools.
- Storage byte preservation across `lite upgrade` is `unsupported` and is rejected before any mutation when required (not silently skipped).
- The L13 hosted cleanup gate proves the measured owned-resource census (public tables, auth users, Storage buckets, SupaDiff ownership schema) returns to the pre-run empty state — not that the hosted project is byte-for-byte identical to its initial image.
- `pnpm release:evidence` verifies the recorded acceptance results for consistency, non-failure, and freshness against the release-inputs digest; it does not itself re-run the gates.

## Scenario fixtures

- `scenarios/deterministic/peer-data-auth-rls-smoke.json` — `scn.peer-data-auth-rls-smoke` · digest `sha256:fb451d1444111fce82f360392a15ad4a236ab77d7d4f95c8b676b2dfb0e4ae60`
- `scenarios/deterministic/supalite-auth-rls-smoke.json` — `scn.supalite-auth-rls-smoke` · digest `sha256:9e3f1acc13f054f44c3ca266b8daa6f91c0b21239af4828b80390013d02e088c`
- `scenarios/deterministic/supalite-data-smoke.json` — `scn.supalite-data-smoke` · digest `sha256:3f821c6a1ea2dc692df034cb241f9fe75d874e3aedba07004d1f05500530ae1f`
- `scenarios/deterministic/supalite-storage-smoke.json` — `scn.supalite-storage-smoke` · digest `sha256:52f881d0cbf40f9b6e2505b57d2a57d89bf885782c4925a6ec086238044a2bdd`
